# 循迹小车物理参数核对清单

**日期**: 2026-07-30  
**目的**: 确保所有物理参数与实际硬件匹配，为平稳循迹做准备

---

## 🔧 已修复的问题

### ✅ 编码器PPR不一致（已修复）

**问题描述**：
- modules/Sens-Decision/src/config.c 使用 pulses_per_revolution = 1024
- modules/MotionControl/inc/motion_config.h 使用 ENCODER_PPR = 334
- 导致状态评估器速度计算错误 **3倍**

**修复内容**：
- 统一使用实际标定值 334
- 同时修正轮半径为 0.033m（与motion_config.h一致）

---

## 📏 需要你测量和核对的物理参数

### 1. 车辆底盘尺寸（优先级：⭐⭐⭐）

| 参数 | 当前配置值 | 测量方法 | 文件位置 |
|------|-----------|----------|----------|
| 轮距 | 0.15 m (150mm) | 用卷尺测量左右轮接地点中心距离 | config.c:26 |
| 轮半径 | 0.033 m (33mm) | 测量轮子外径÷2，或周长÷2π | config.c:34, motion_config.h:28 |

**实测记录表**：
```
轮距实测值:     _______ mm  (当前配置: 150 mm)
左轮半径实测值:  _______ mm  (当前配置: 33 mm)
右轮半径实测值:  _______ mm  (当前配置: 33 mm)

是否需要更新配置: □ 是  □ 否
```

---

### 2. 编码器参数验证（优先级：⭐⭐⭐）

| 参数 | 当前值 | 验证方法 | 文件位置 |
|------|--------|----------|----------|
| 编码器分辨率 | 334 counts/rev | 手动转一圈验证 | config.c:35 |
| 编码器方向 | 左+1, 右-1 | 正转时计数增加为正 | config.c:21 |

**验证记录表**：
```
左轮转1圈计数:  _______ counts (预期: 334)
右轮转1圈计数:  _______ counts (预期: 334)

是否需要更新配置: □ 是  □ 否
```

---

### 3. 传感器安装位置（优先级：⭐⭐）

| 传感器 | X (前后, m) | Y (左右, m) | Z (高度, m) | 文件位置 |
|--------|------------|------------|------------|----------|
| IR阵列 | 0.10 | 0.0 | -0.02 | config.c:58-60 |
| IMU | 0.0 | 0.0 | 0.03 | config.c:50-52 |

---

## 🎛️ 需要实车标定的控制参数

### 4. 前馈控制参数（优先级：⭐⭐⭐）

**位置**: modules/MotionControl/inc/motion_config.h:97-104

| 参数 | 当前值 | 标定方法 |
|------|--------|----------|
| FF_K_STATIC | 80 PWM | 从0增加PWM直到轮子刚转动 |
| FF_K_FRICTION | 300 PWM/(m/s) | 恒速行驶时PWM/速度 |

**标定记录表**：
```
静摩擦标定:
  临界PWM值: _______ (当前配置: 80)

动摩擦标定:
  PWM=150 → Speed=_____ m/s → Ratio=_____
  PWM=200 → Speed=_____ m/s → Ratio=_____
  PWM=250 → Speed=_____ m/s → Ratio=_____
  平均比值: _______ (当前配置: 300)
```

---

### 5. 轮速PI控制器（优先级：⭐⭐⭐）

**位置**: modules/MotionControl/inc/motion_config.h:72-75

| 参数 | 当前值 | 调参方法 |
|------|--------|----------|
| SPEED_KP | 200 | 从50开始递增到无震荡 |
| SPEED_KI | 50 | KP调好后加入，消除稳态误差 |

**调参记录表**：
```
比例控制调试:
  KP=50  → 响应: □慢 □快 □震荡
  KP=100 → 响应: □慢 □快 □震荡
  KP=150 → 响应: □慢 □快 □震荡
  KP=200 → 响应: □慢 □快 □震荡
  
  推荐 SPEED_KP = _______

积分控制调试:
  KI=10 → 稳态误差: _____ m/s
  KI=30 → 稳态误差: _____ m/s
  KI=50 → 稳态误差: _____ m/s
  
  推荐 SPEED_KI = _______
```

---

### 6. IR循迹修正增益（优先级：⭐⭐⭐）

**位置**: Core/Src/app/control_app.c:187-190

| 参数 | 当前值 | 推荐范围 |
|------|--------|----------|
| lateral_gain | 2.0 | 0.5 - 3.0 |
| heading_gain | 1.5 | 0.3 - 2.0 |
| max_omega_radps | 2.0 rad/s | 1.0 - 3.0 |

**调参记录表**：
```
速度 0.3 m/s:
  lateral_gain=0.5 → 表现: □迟钝 □合适 □摇摆
  lateral_gain=1.0 → 表现: □迟钝 □合适 □摇摆
  lateral_gain=1.5 → 表现: □迟钝 □合适 □摇摆
  lateral_gain=2.0 → 表现: □迟钝 □合适 □摇摆
  推荐值: _______

  heading_gain=0.0 → 超调: □大 □中 □小
  heading_gain=0.5 → 超调: □大 □中 □小
  heading_gain=1.0 → 超调: □大 □中 □小
  heading_gain=1.5 → 超调: □大 □中 □小
  推荐值: _______
```

---

### 7. 速度规划参数（优先级：⭐⭐）

**位置**: modules/Sens-Decision/src/config.c:82-86

| 参数 | 当前值 | 调试建议 |
|------|--------|----------|
| line_speed_mps | 1.0 m/s | 直线段速度，从0.3开始递增 |
| curve_speed_mps | 0.5 m/s | 过弯速度 |

**调试策略**：
```
第1阶段：低速稳定
  line_speed = 0.3 m/s, curve_speed = 0.2 m/s

第2阶段：中速优化
  line_speed = 0.6 m/s, curve_speed = 0.4 m/s

第3阶段：高速冲刺
  line_speed = 1.0 m/s, curve_speed = 0.7 m/s
```

---

## ✅ 参数更新检查表

完成测量和标定后，需要更新以下文件：

### 文件1: modules/Sens-Decision/src/config.c
- 第26行：轮距 wheel_track_m
- 第34-35行：编码器参数（已修复）
- 第82-86行：速度规划

### 文件2: modules/MotionControl/inc/motion_config.h
- 第25行：轮距 WHEEL_BASE
- 第28行：轮半径 WHEEL_RADIUS
- 第72-75行：PI参数
- 第98-104行：前馈参数

### 文件3: Core/Src/app/control_app.c
- 第187-190行：IR修正增益

---

## 🎯 调试顺序建议

1. 物理参数测量 (1小时)
2. 底层控制标定 (2-3小时)
3. 循迹参数调优 (2-3小时)
4. 整体性能优化 (1-2小时)

**总计预估时间**: 6-9小时

---

**创建时间**: 2026-07-30  
**状态**: ✅ 准备好进行实车测试
