# Scripts

This directory contains helper, debug, preview, and test entry points.

Current script entry points live under `scripts/test/`.

Core algorithm logic should live under `src/vision/`, not in scripts.
